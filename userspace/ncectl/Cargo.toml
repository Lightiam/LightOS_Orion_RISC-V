[package]
name = "ncectl"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0 OR MIT"
description = "NCE control: inspect /dev/nce* descriptors, drive power states"

[dependencies]
libc_shim = { path = "../libc_shim" }
