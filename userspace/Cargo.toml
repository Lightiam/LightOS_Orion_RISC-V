[workspace]
resolver = "2"
members = [
    "libc_shim",
    "init",
    "hello",
    "shell",
    "ncectl",
    "selftest",
    "netprobe",
    "httpget",
]

[profile.release]
panic = "abort"
opt-level = "s"
lto = true

[profile.dev]
panic = "abort"
